---
en:
  activemodel:
    errors:
      models:
        bulk_submission_form:
          attributes:
            uploaded_file:
              blank: You must select a file to upload
              content_type_invalid: "%{filename} must be a CSV"
              file_empty: "%{filename} is empty"
              file_too_big: "%{filename} is more than %{size}MB"
              file_too_long: "%{filename} has more than 35 records"
              missing_first_name: "%{filename} first name missing at row %{row_num}"
              missing_last_name: "%{filename} last name missing at row %{row_num}"
              invalid_nino: "%{filename} invalid national insurance number at row %{row_num}"
              invalid_dob: "%{filename} invalid date of birth at row %{row_num}"
              invalid_period_start_date: "%{filename} invalid period start date at row %{row_num}"
              invalid_period_end_date: "%{filename} invalid period end date at row %{row_num}"
              period_end_date_before_start_date: "%{filename} period end date earlier than period start date at row %{row_num}"
